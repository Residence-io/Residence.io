export default async function NewStaffPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Staff Registration — Loading Test</h1>
      <p>If you can see this, the page routing works.</p>
    </div>
  );
}
